class Contato {
  nome: string
  categoria: string
  id: number

  constructor(nome: string, categoria: string, id: number) {
    this.nome = nome
    this.categoria = categoria
    this.id = id
  }
}
export default Contato
