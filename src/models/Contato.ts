class Contato {
  contato: string
  nome: string
  categoria: string
  id: number

  constructor(contato: string, nome: string, categoria: string, id: number) {
    this.contato = contato
    this.nome = nome
    this.categoria = categoria
    this.id = id
  }
}
export default Contato
